# 发布指南（东鸿密钥小程序）

本项目是**原生微信小程序**，无后端、无网络请求、无登录、无支付，审核风险低。
发布前必须先完成**主体认证**与**小程序备案**——未备案提交发布会报错 `86369`。

项目现状（可直接用于发布）：

| 项目 | 值 |
| --- | --- |
| AppID | `wxce58a35a420f7778`（已写在 `project.config.json`） |
| 代码目录 | `miniprogram/`，14 个页面 |
| 包体积 | 约 396 KB（主包上限 2 MB） |
| npm 依赖 | 无（`node_modules` 只给格式化和测试用） |
| 网络请求 | 无，不需要配置服务器域名 |
| 隐私接口 | 无（只用本地存储、跳转、提示），不需要用户隐私协议弹窗 |
| 建议基础库 | 3.7.0 以上（`project.private.config.json` 已设 3.17.2） |

## 一、发布前的一次性准备（在微信公众平台）

1. 登录 [mp.weixin.qq.com](https://mp.weixin.qq.com)，确认主体已认证（企业文化内容建议用**企业主体**），并核对 AppID 与项目一致。
2. **小程序备案**（强制的上线前置条件）：公众平台 → 设置 → 基本设置 → 小程序备案 → 填写主体与负责人信息 → 平台初审（1–2 个工作日）→ 工信部短信核验 → 通信管理局审核（1–20 个工作日）。备案通过前可以先用体验版给同事试用。
3. 填写**名称、头像、简介**，内容要与实际功能一致（避免「服务内容与名称不符」被驳回）。
   建议简介：`东鸿企业文化探索工具：阅读企业文化、完成互动关卡，数据仅存本地。`
4. 选择**服务类目**（以后台实际可选项为准，需与内容匹配）：
   - 内部培训/学习类内容，通常选「教育 > 教育信息服务」或「商业服务 > 企业管理服务」；
   - 若只想当工具，可选「工具 > 效率」。
   类目与服务内容不符是常见驳回原因，被驳回时按提示补说明或调整类目。
5. **成员管理**：把自己的微信号加为「开发者」（才能上传代码）；要试用的同事加为「体验成员」。
6. 若只想**企业内部使用**，还可在后台关闭「通过名称搜索」，或使用微信开发者工具的「仅在企业微信运行」选项（注意：该模式下不能在微信里打开或被搜索）。

## 二、上传代码

1. 打开微信开发者工具 → 导入项目 → 选择本目录（`donghong-key`），确认右上角 AppID 正确。
2. 详情 → 本地设置：勾选「ES6 转 ES5」「压缩代码」（`project.config.json` 已配好 `es6` / `minified` / `minifyWXSS` / `minifyWXML`）。
3. 上传前先在模拟器和真机各走一遍下面的「自检路径」。
4. 点右上角「上传」→ 版本号填 `1.0.0`，备注填 `首个版本：企业文化 + 互动关卡 + 小游戏`。
5. 若提示「不在项目成员中」，回公众平台把当前微信号加入开发者，重新登录工具再传。

## 三、提交审核

1. 公众平台 → 管理 → 版本管理 → 开发版本 → 「提交审核」。
2. 填写：
   - 功能页面：`pages/home/home`
   - 测试账号：不需要（无登录）
   - **审核备注**（直接复制下面这段，能明显降低沟通成本）：

     ```
     本小程序为企业内部企业文化学习工具，面向员工自学使用，无任何商业化内容。
     共 14 个页面：首页 → 企业文化（内容阅读 + 打卡答题 + 互动关卡）→ 闯关小游戏 → 通关结算。
     小程序无登录、无支付、无广告、无用户生成内容（UGC），不采集任何个人信息，
     所有进度仅保存在用户本机 Storage，不发起任何网络请求，未使用定位、相册、麦克风等
     敏感权限。使用方式：打开首页 → 点「企业文化」按引导操作即可完整体验全部功能。
     ```

3. 提交后审核一般 1–7 个工作日；如被驳回，看驳回理由对应处理（常见：类目不符、名称与内容不符、缺少资质说明）。

## 四、发布

1. 审核通过 → 版本管理 → 审核版本 → 「发布」。
2. 发布后可随时回滚：版本管理里能把已发布版本「退回」。
3. 想更新：改代码 → 上传（版本号递增，如 1.0.1）→ 提交审核 → 发布。

## 五、发布前自检路径（模拟器 + 真机各走一遍）

- [ ] 首页 →「开启文化探索」→ 序章三段 → **领取使命密钥** → 进入质量值班（能正常跳转）。
- [ ] 值班六份材料：每份点一次「通过」或「退回」，结果页显示信任值与后果，第六份后进入文化画像。
- [ ] 首页 →「企业文化」→ 四个展区打卡（弹层：介绍 → 答题 → 结果）→ 三个互动关卡（值班 / 认证配对 / 方案组卡）。
- [ ] 首页 →「闯关小游戏」：模块未完成时是锁定的；完成后能进跳格子（答对前进、答错后退）与另外四个小游戏。
- [ ] 杀掉小程序再进来：进度、信任值、各游戏成绩都还在。
- [ ] 探索档案 →「清除本机存档」取消不清、确认清空。
- [ ] 不同机型看一遍排版：小屏（如 iPhone SE / 安卓小屏）不出现文字被裁；切换深色模式不影响配色。

## 六、常见问题

- **点了按钮没反应**：几乎都是跳转到了不存在的页面。`npm run check` 会扫描所有 `/pages/...` 路径是否真的存在（39 条），提交审核前跑一次。
- **发布报 86369**：备案未完成，先去公众平台提交备案。
- **审核说类目不符**：调整服务类目，或在审核备注里说明是内部企业工具。
- **想先给同事看**：不用等审核，上传后在后台把同事加为「体验成员」，发体验版二维码即可（体验成员上限以后台显示为准）。
