const pager=require('../../utils/pager');
const c=require('../../data/content');const store=require('../../utils/storage');const state=require('../../utils/state');
Page({...pager.methods,data:{panel:0,panelCount:3,culture:c.culture,actions:c.actions,selected:''},onShow(){const s=store.read();if(!s.innovated){wx.redirectTo({url:state.route(s)});return;}this.setData({s,selected:s.pledge,warning:store.warning()});},choose(e){this.setData({selected:e.currentTarget.dataset.value});},save(){if(!this.data.selected)return;const s=store.dispatch('complete',this.data.selected);this.setData({s,warning:store.warning()});if(!store.warning())wx.showToast({title:'文化印记已保存',icon:'success'});},home(){wx.reLaunch({url:'/pages/home/home'});},progress(){wx.redirectTo({url:'/pages/progress/progress'});}});
