# 验证记录

## 本次已验证

- 通过 `miniprogram-compiler@0.2.3` 封装的 WCC / WCSC 编译器完成全部 WXML / WXSS 编译检查。该包内置编译器较旧，此检查不能替代当前微信开发者工具验收。
- 编译后的 WXML 在 12 种首页、序章、章节、复测、强化和档案状态下成功生成节点树。该验证不检查真实设备布局与触摸事件。
- Node.js v24.16.0 执行 `npm test`：10 项自动化测试通过。
- `npm run check`：5 个页面完整，31 个原生源码文件的 JS / JSON 语法和页面事件绑定检查通过。
- 关卡前置条件、完整通关、错误重试、模块去重、存档序列化恢复、损坏数据降级通过。
- 页面控制器模拟：首页续玩、序章到章节、点选组装、拖放命中/外部释放/取消、异常排查、文化承诺通过。
- 本地存储写入失败的提示、内存保留和重试通过。

## 代码风格整理验证

- `npm run verify`：Prettier `--check` 全部通过、WXML 排版器报「需要排版 0 个文件」、`npm run check` 与 17 项测试继续全绿。
- JS / JSON / WXSS 只改空白与换行：对旧文件与新文件分别跑一遍 Prettier，输出逐字节相同。
- WXML 逐模板做「语义对比」（标签含属性序列 + 非空文本节点序列）：8 个模板全部一致，0 处差异；排版器连跑两次结果相同（幂等）。
- 排版器对 7 处「含文本 / 相邻行内元素、不能安全折行」的位置给出提示并保留原样，未强行折行。
- 人工改动（变量名写全、拆开单行多变量声明、嵌套三元改 `if` 早返回）均为等义重写，由既有 17 项测试覆盖。

## 真机问题修复：序章领密钥无反应（2026-09-16）

- 现象：真机上点「领取使命密钥」没有任何反应。
- 原因：`pages/prologue/prologue.js` 里写死跳转到 `/pages/chapter/chapter`，而该页在改为「质量值班」时已删除；真机上跳到不存在的页面会静默失败，所以按了没反应。自动化测试当时只断言了存档状态，没断言跳转目标，因此漏掉了。
- 修复：改为按存档跳转 `state.route(...)`（正常进入 `/pages/shift/shift`）；任务清单里 flip / crush / quiz 的页面路径也一并改成各自的真实页面。
- 防呆：`tests/check.js` 新增「页面路由检查」——扫描所有 JS 里的 `/pages/...` 字面量，必须存在于 `app.json` 的 pages 列表（39 条），并反向检查每个页面都有入口；故意写回旧路径验证过会报错。
- 测试：新增 1 条断言（领完密钥后跳转目标为 `/pages/shift/shift`），共 49 项全绿。

## 跳格子改成跳一跳式等距场景（2026-09-17）

- 视觉：等距 3D 平台（方台 / 圆柱 / 条纹台三种造型轮换，台面刻格号）、棋子（圆柱身 + 小球）、地面投影、灰白渐变背景、左上角大数字显示当前格；平台逐格往右上排开，只渲染当前格前后共 5 块（相机跟随），桌面端场景居中限宽 1080px。
- 手感：按住工作栏蓄力 → 指针按三角波上下往返（240ms/项）→ 松手选中；选对时棋子做抛物线跳（`--fx/--fy/--mx/--my/--tx/--ty` + `@keyframes hop-arc`），选错原地 `hop-bump` 抖动并提示重选。
- 实测（414×896）：场景含 16 段平台路径 + 棋子 + 台面格号 1..4 + 左上角分数；按住 240×n+40ms 命中第 n 项；答对后格子 +1、棋子 `transform` 从 (340,282) 跳到 (340,310) 且带 `jump` 动画；答错停在原格。
- 端到端测试同步（按压时长仍按目标下标计算，反馈文案改为「重新选一次」）。

## 玩法与首页改版（2026-09-16 深夜）

- **文化跳格子改为「跳一跳」式按压选择**：上方空白处显示问题，下方工作栏竖排四个答案与一条指针；按住工作栏时指针按三角波上下往返（每 240ms 扫过一项），松手选中指针所在项。实测按住 0/240/480/720ms 分别停在 0..3 项上，指针 top 依次 5→122→239→355。
- **规则调整**：答错停在原格重选（不再退回一格），共享状态机同步改（小程序与网页一致）。
- **模块配对改为牌面全部正面朝上**：点两张直接配对，配错闪红，不再需要翻牌。
- **删除复现异常游戏**：网页与小程序两端一起移除（页面、内容、状态、任务、成绩、样式、测试），实验台组件一并删掉。
- 首页改成铺满宽度的落地页：hero（标题 + 网络图 + 使命 + 继续按钮）+ 三块内容按顺序横排（带箭头与状态）+ 方向/价值观/进度；桌面 1440px 下外壳 1240px、hero 三栏、流程三块并排、无横向溢出。
- 端到端断言增至 108 项（含按压选择、正面配对、桌面布局）。

## 结构重排 + 跳格子验证（2026-09-16）

- 按「先企业文化、再闯关」重排：首页只剩两个入口（企业文化 / 闯关小游戏）；`pages/quest` 改成企业文化模块（打卡弹层分「介绍 → 答题 → 结果」三步、三个互动关卡、模块完成度），新增 `pages/games`（小游戏区，模块未完成时全部锁定）与 `pages/hop`（文化跳格子）。
- 跳格子规则：站在格子上先看知识点，答对前进一格、答错退回一格（起点夹在 0，不会退成负数），12 格走到终点通关；对错次数与位置写入存档，中途退出可续。
- 新增 3 项状态测试（前进/后退/夹紧/通关、伪造记录夹回合法范围、模块完成度决定解锁）与 2 项页面测试（小游戏区锁定与解锁、跳格子前进后退到通关）。
- 自动化测试 49 项全绿；静态检查 73 个源文件、14 个页面通过。
- 用 Chromium 量过企业文化页、打卡弹层三个状态、小游戏区、跳格子页，在 320×560 / 375×600 / 414×736 下**零溢出**（弹层按「介绍 / 答题 / 结果」拆三步就是为了在矮屏也放得下）。
- **量布局的坑**：入场动画（`scale(0.96)`）未结束时测 `getBoundingClientRect()` 会得到偏小的高度，`scrollHeight - clientHeight` 会假报 10~20px 溢出；之前记的「结算弹层 17px 内部滚动」就是这个假象，实际为零，需等动画结束（约 700ms）再量。

## 复现异常关卡验证（2026-09-16）

- 把一直闲置的 `components/live-meter` 接进闯关：新关卡「复现异常」要先通电、再把负载推到 85% 以上并保持 3 秒；负载掉下来会重新计时，低负载与断电都不计数。
- 组件新增 `loadchange` 事件把负载与通电状态抛给页面；组件测试补了 `triggerEvent` 桩，原有用例继续通过。
- 存档新增 `games.repro`（负载越界会夹到 0~100，低于阈值一律作废），任务轨道变成 13 项。
- 新增 1 项状态测试与 1 项页面测试（断电提示 / 低负载不计数 / 达阈值开始计时 / 掉下来清零 / 走完通关）。
- 自动化测试 44 项全绿；静态检查 65 个源文件、12 个页面通过。

## 二、三章换成小游戏验证（2026-09-16）

- 第二章改为「认证配对」（市场 × 认证卡）、第三章改为「预算组卡」（六张能力卡选三张，含必需项校验与满分组）；删除旧的 NPC 三选一页面 `pages/mission`，相关内容块一并清理。
- 存档升到 v5：`missions` 改为由两个小游戏的记录推导，不再直接读存档；v4 及更早存档里的章节完成状态会迁移成完整的小游戏记录（旧档不会丢进度）。
- 新增 4 项状态测试（错配不点亮、重复提交不计数、缺必需项被打回、v4 迁移）与 3 项页面测试（认证配对全流程、方案组卡预算与反馈、章节从值班闭环贯通）。
- 自动化测试 42 项全绿；静态检查 61 个源文件、11 个页面通过。
- 用 Chromium 量过两个新页面在 320×560 / 375×600 / 414×736 下均无溢出。

## 闯关中心改造验证（2026-09-16）

- 参考线上博物馆式闯关站点，新增闯关中心：11 项任务轨道、四个展区打卡答题、三个小游戏（配对 / 三消 / 答题）、全部通关结算与 GRADE 评级；新增 `pages/quest`、`pages/flip`、`pages/crush`、`pages/quiz` 与 `utils/match3.js`。
- 存档升到 v4：新增 checkins 与 games；小游戏成绩越界（配对步数 99999、三消负分、答题记录不完整）都会被清洗，答题分数按记录重算而不是读存档里的数字。
- 自动化测试 38 项全绿（新增 7 项三消纯逻辑、5 项状态机、5 项页面流程）。
- 用 Chromium 按真实文案与真实 WXSS 量过：闯关中心任务网格在 320×560 ~ 414×736 五种视口下**零溢出**；翻牌 4×4 改用显式行高后同样零溢出。
- 过程中修掉两个真 bug：① 翻牌「还没翻开第一张」用 0 当标记，导致第一张牌在索引 0 时配对错乱（改用 -1 哨兵）；② `findMatches` 按方阵扫描，非方阵会读到 undefined 误判三连（改为按行长扫描）。
- 待真机确认：三消手感与动画节奏、翻牌翻转的 3D 效果、emoji 在低端机上的渲染。

## 游戏化改造验证（2026-09-16）

- 把第一章从「七个分屏的任务关卡」换成「一屏一张事件卡的盖章值班」，新增 `pages/shift/`，删除 `pages/chapter/`。
- 存档升到 v3：只记录每次盖章的选择，信任值与四项印记由决定重算；旧版（v1/v2）与伪造存档均安全回退，不会解锁未完成的进度。
- 新增 4 项状态测试（信任值上下限与重算、延迟后果变体、伪造决定截断、画像分档）与 3 项页面测试（盖章闭环、滑动盖章、三选一卡片不误盖）；共 **21 项通过**。
- 排版脚本对比：用 Chromium 以真实文案渲染 13 种卡片状态 × 4 种视口（320×640 / 320×560 / 375×600 / 414×736），材料区与整页溢出均为 **0**（页面禁用滚动，溢出即被裁切）。
- 待模拟器 / 真机确认：印章动画、滑动盖章手感、小屏下材料字号是否好读。

## 待开发者工具 / 真机验证

初版测试没有启动真实微信运行时；后续更新也尚未完成模拟器视觉与真机验收。

- [ ] 导入项目后无编译或运行错误，六个页面正常显示。
- [ ] 首页与序章 Canvas 网络在不同像素密度下清晰显示。
- [ ] iPhone / Android 上拖放坐标准确；滚动后、手势取消后仍正常。
- [ ] 小屏设备组装区和模块区可操作，文字不溢出。
- [ ] 每个阶段退出小程序再进入，保持最新动作；已装模块保持点亮。
- [ ] 错误装配有对应解释；重复拖放不重复计数。
- [ ] 未完成拆解不能组装、未组装不能测试、未复测不能进入创新挑战。
- [ ] 文化印记未保存行动承诺时进度为 90%；保存后 100%。
- [ ] 清档弹窗取消时保留原进度，确认时恢复初始状态。
- [ ] 微信后台切回及屏幕尺寸变化后 Canvas 与拖拽仍正常。

## 教学内容验收

- [ ] 企业文化负责人确认愿景、使命、理念、导向与价值观解释。
- [ ] 产品工程师确认简化模块及教学描述不会引起真实产品规格误解。
- [ ] 员工试用后能描述「创新驱动、技术为本」如何体现在组装、追溯与方案选择中。

## 动态互动更新验证

12 项自动化测试通过，新增通断电、负载联动以及动画后台暂停/卸载清理测试；更新后的 7 个 WXML 模板与 WXSS 编译通过。动画帧率、显示效果与手势响应仍需微信真机验证。

## 固定窗口与天蓝主题验证

- 16 项自动化测试通过：新增横向手势识别、纵向/短距/取消手势忽略、边界限制、拖拽与滑杆区域隔离、关卡解锁、回看不回退存档。
- 全部 5 个原生页面设置 disableScroll，未引入 scroll-view；内容拆为 20 个固定分屏。
- 7 个 WXML 模板与全部 WXSS 编译通过，20 个分屏状态生成节点树通过。
- 针对窗口高度 600px、520px 设置紧凑布局，覆盖较小手机窗口；没有把内容转换成 H5。
- 待模拟器/真机检查：底部安全区、小屏内容不裁切、触摸切换、滑杆不误翻页、组装拖拽准确、字体放大后的布局。

## 跳格子手感微调 + 删除能量三消（2026-09-17 晚）

- **平台同步下压**：调答案时，棋子下蹲的同时脚下的平台也围绕自身底部压缩（`scaleY(1 − 0.3·蓄力)`，棋子按平台顶面下沉量 translateY 跟随）；实测蓄力 0.31 → 棋子 0.90 / 平台 0.915，蓄力 0.63 → 棋子 0.80 / 平台 0.82，同步下沉 5.6px / 11.3px。
- **蓄力放慢**：蹲到位时间 900ms → 1600ms，指针节拍 240ms/项 → 360ms/项（约 4 趟扫完四个选项）。
- **跳完自动进格**：答对后不再出现「跳上下一格」按钮，显示「正在进入下一格…」并在 1.2 秒后自动进入下一格（最后一格则回到闯关中心）；答错仍保留「重新选一次」按钮。
- **删除能量三消**（网页 + 小程序两端）：页面、内容（`content.match3`）、纯逻辑 `utils/match3.js`、状态机 `games.crush` 与 `crushResult`、任务项、结算成绩、档案页成绩行、样式、测试一起移除；GRADE S/A 不再要求三消分数（S：信任 ≥85 + 答题 ≥80 + 配对 ≤20 步）。
- 小程序侧：60 个源文件、12 个页面、34 条路由，36 项测试全绿；网页侧 15 条路由、3 个小游戏。

## 「重新开始」入口（2026-09-17）

- **网页版**：首页底部和探索档案页都加了「重新开始（清空记录）」。点击改成**两段式确认**——第一次点按钮文案变成「确定清空？再点一次（6 秒内）」（按钮变红），不点就 6 秒后自动复位；再点一次才 `DHKStore.reset()`、回到首页并提示「记录已清空，从头开始吧。」。**不再用 `window.confirm`**（内嵌/演示环境里会被拦或很难看），改成渲染下一条消息的一次性 `App.flash()`。
- **小程序版**：首页补了同样的入口，用系统 `wx.showModal` 二次确认，确认后清档 + 「已清空，从头开始」提示；档案页原有入口保留。
- **测试**：网页端到端新增 6 项断言（入口存在、第一次点只改文案不清档、第二次点后存档与「全新存档」逐字段一致、回到首页并有提示、档案页入口也在），合计 **171 项**。

## 「重新开始」按钮位置 + 模块配对牌面修复（2026-09-17）

- **重新开始**：从页面底部那行低调文字改成首页顶栏里的红色圆角按钮，就排在「文化探索员」称号左边（小程序版同步挪到顶栏）。第一次点变成「点这里确认清空」（`data-armed-label`），6 秒内再点一次才生效。
- **模块配对牌面修复**：网页版之前只把牌改成正面朝上，**没有套用牌面容器**——卡片的背景、描边、圆角都在小程序 `.face.back` 上，于是网页版只剩两行 10px 小字贴在页面底色上，既看不出是牌、也没有选中反馈，所以「选了没反应、配不起来」。现在网页版牌面结构与小程序一致（`.card.flipped > .inner > .face.back`，用 `flipped` 把牌背转正），并补了网页专属样式：牌面文字 12px、emoji 22px、选中亮蓝框、配错闪红、配好变绿。
- **测试**：新增「牌面看得清、能点」「点一张牌会高亮」「重新开始按钮在顶栏且挨着文化探索员」等断言，端到端合计 **173 项**；360×640 / 390×844 / 414×896 / 768×1024 / 1280×720 / 1440×900 六种视口下 16 张牌全部可见且点击命中自身。

## 网页版字号整体放大一档（2026-09-17）

- **问题**：小程序 WXSS 里的 px 是按手机屏定的，其中 35 处 10px、29 处 11px、25 处 12px；搬到浏览器（尤其桌面）显得又小又碎，手机上因为屏小更显局促。
- **做法**：在 `tools/build-web.mjs` 转译 WXSS 时统一放大字号（新增 `webFontSize()` / `scaleType()`）——只动 `font-size`、px 的 `line-height`、`letter-spacing` 以及 `font:` 简写里的字号，**不动 padding/height 等布局尺寸**，所以各页版式不变；`web.css` 里的手写字号同步抬档。映射：≥30px +2 / ≥18px +1.5 / ≥14px +1.5 / ≥12px +2 / 11px → 13.5px / 其余 → **13px 保底**。
- **滚动兜底**：字号变大后极小屏可能放不下一屏，`.app-shell.viewport` 由 `overflow: hidden` 改为 `overflow-y: auto`（原来首页在手机上其实是**被裁掉、滚不动**的）。
- **测试**：端到端布局断言升级为「内容不被裁掉（放不下就必须能滚）＋ 每屏最小字号 ≥13px ＋ 无横向溢出」，覆盖 14 条路由；合计 **201 项断言**。量得最小字号从 9–10px 提升到 13px（正文 15.5px、标题 18.5–28.5px）。
